# Myanmar ASR & TTS Tutorial — Python Dependencies
# Install with:  pip install -r requirements.txt

# ── Core deep learning ─────────────────────────────────────────────────────
torch>=2.0.0
torchaudio>=2.0.0

# ── ASR: HuggingFace stack ─────────────────────────────────────────────────
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.24.0          # speeds up Trainer on multi-GPU / CPU offload

# ── Audio processing ───────────────────────────────────────────────────────
soundfile>=0.12.1            # fast WAV I/O
librosa>=0.10.0              # mel spectrograms, Griffin-Lim vocoder

# ── Visualisation & reporting ──────────────────────────────────────────────
matplotlib>=3.7.0

# ── General numerics ───────────────────────────────────────────────────────
numpy>=1.24.0

# ── Optional: TensorBoard logging (--tensorboard flag) ────────────────────
# tensorboard>=2.14.0

# ── Optional: Myanmar syllable segmenter (improves CER tokenisation) ──────
# pyidaungsu>=1.2.3          # pip install pyidaungsu

# ── Optional: automatic MOS proxy for TTS evaluation ──────────────────────
# utmos                      # pip install utmos
#   see https://github.com/sarulab-speech/UTMOS22

# ── Optional: ESPnet for production-quality TTS / ASR ─────────────────────
# espnet>=202310             # pip install espnet espnet_model_zoo
